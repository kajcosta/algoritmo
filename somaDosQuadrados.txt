//Elaborar um programa que efetue a leitura de três valores (A, B e C) e apresente como resultado final à soma dos quadrados dos três valores lidos
programa {
  funcao inicio() {
  
     inteiro a, b, c, somaQuadrados 

    escreva("Valor de A: ")
    leia(a)
    
    escreva("Valor de B: ")
    leia(b)
    
    escreva("Valor de C: ")
    leia(c)

    somaQuadrados = (a*a)+(b*b)+(c*c)
    
    escreva("A soma dos quadrados dos valores é: ", somaQuadrados)


  }
}